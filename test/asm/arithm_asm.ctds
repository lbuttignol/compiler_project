class Main {
	integer print_int(integer x) extern;
	void main() {
		integer a,b,c;
    	integer i;
		a=10;
		b=5;
		c= a+b*a*b/a*a+b-a+b+b-a/a*b;
		/*print_int(a+b*a*b/a*a+b-a+b+b-a/a*b);
		print_int(c);
		print_int(a/b);
		print_int(b/a);
*/
		i=1;

		while(true){

    if (i<=5){
		  print_int(i);
          i+=1;
          while(true){
          		print_int(654);
             break; 
          }
          continue;
		  }
		  else{
		  	break;
		  }
		}
	}
}