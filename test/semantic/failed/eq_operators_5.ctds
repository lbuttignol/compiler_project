class Main{
	void main(){
		integer a,b;
		float d,c;
		bool e,f;
		if (a==b)
		{
			if (c==d)
			{
				if (a==f)
				{
					if (a!=b)
					{
						if (d!=c)
						{
							if (e!=f)
							{
								a = 65;
							}
						}
					}
				}
			}
		}
		// a and c shuld have the same type
		if (a!=c)
		{
			a = 654;
		}
		return;
	}
}