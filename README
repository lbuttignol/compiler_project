La documentación se encuentra en el directorio ../compiler_project/doc/

Instrucciones

--Ubicarse en el directorio ../compiler_complete/ en el terminal

--Limpiar los archivos de compilaciones anteriores (opcional, just in case)
	
	*********************************************************
	* $ ant clean                                           *
	*********************************************************

--Compilar
	Correr el siguiente comando para compilar

	*********************************************************
	* $ ant                                                 *
	*********************************************************

--Correr tests
	Para ejecutar toda la batería de tests se inluyó un script en el directorio, 
	para ejecutar el mismo, utilizar el siguiente comando

	*********************************************************
	* $ sh runTests.sh                                      *
	********************************************************* 
