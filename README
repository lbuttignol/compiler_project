- Compilar 

   >ant

Luego de que se genero el analizados léxico y sintáctico , compila y arma un JAR, llamado "Compiler.jar", dentro de la carpeta  "dist/".

- Para ejecutar:

   > java -jar dist/Compiler.jar input.test

en el archivo input.test, tienen 3 expresiones.. 2 de las cuales son correctas y una tiene un error sintáctico. En este ejemplo, muestra el resultado de la expresión si es correcta, caso contrario informa el Error.

4+4;
5++5; -->ERROR 
5+5*2;


- Para ejecutar el script con los test:

   > sh runTest.sh
